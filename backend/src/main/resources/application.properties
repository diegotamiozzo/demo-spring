spring.application.name=demo-spring

# ---------------------------------------------------------------------------
# Banco de dados (MySQL)
# ---------------------------------------------------------------------------
spring.datasource.url=jdbc:mysql://173.255.119.217:3306/demo_spring?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=tamiozzo
spring.datasource.password=Corcel@73

# ddl-auto: update = cria/atualiza tabelas automaticamente a partir das entidades.
# Use 'validate' ou 'none' em produção e controle o schema via migrations.
spring.jpa.hibernate.ddl-auto=${DB_DDL_AUTO:update}
spring.jpa.show-sql=${DB_SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Console H2 desativado (não se aplica ao MySQL)
spring.h2.console.enabled=false

# Porta do servidor (opcional, configurável via .env)
server.port=${SERVER_PORT:8080}
